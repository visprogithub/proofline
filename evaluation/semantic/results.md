# Semantic evaluation result

**Status:** NOT RUN — production integration is not permitted.

The locked fixtures and deterministic outperform gate exist, but no embedding runtime has been approved or evaluated. Proofline therefore adds no production embedding dependency, model download, semantic association rule, or semantic UI in this iteration state.

This is a valid conditional Phase 3 outcome until a separately reviewed candidate evaluation records baseline and candidate precision, recall, F1, model ID, revision, dtype, threshold, asset size, and benchmark environment.
